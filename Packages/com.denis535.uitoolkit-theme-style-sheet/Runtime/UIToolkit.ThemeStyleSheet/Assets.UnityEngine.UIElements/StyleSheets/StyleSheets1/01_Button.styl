@import "../Internal/_Theme"

// selector
selector--button        = // string: .unity-button.{state}
selector--repeat-button = // string: .unity-repeat-button.{state}

// style
.selector--button        // styles: add-text(middle-center, null, null)
.selector--repeat-button // styles: add-text(middle-center, null, null)

// theme
.selector--button        // styles: add-theme(button)
.selector--repeat-button // styles: add-theme(button)

// layout
.selector--button        // styles: add-layout(null, auto, auto, (4px), (1px), (12px 12px 10px 10px))
.selector--repeat-button // styles: add-layout(null, auto, auto, (4px), (1px), (12px 12px 10px 10px))

// flex
.selector--button        // styles: add-flex(0, 1, auto, auto)
.selector--repeat-button // styles: add-flex(0, 1, auto, auto)
